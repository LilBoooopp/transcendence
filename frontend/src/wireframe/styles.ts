export const styles = {
  // Shared transition settings for consistent animation speed across the app
  transition: "transition-all duration-200",
  
  // Common card styling (used for Tiles, Panels, etc.)
  // Includes: Background colors, rounded corners, shadows, and hover effects
  card: "bg-primary hover:bg-primary-hover rounded-xl shadow-md hover:shadow-lg cursor-pointer",
  
  // Flexbox centering helper (centers content vertically and horizontally)
  center: "flex flex-col items-center justify-center text-center",
};